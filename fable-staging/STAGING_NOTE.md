# TEMPORARY STAGING COPY

This directory is a persistence copy of the dedicated Fable repo (founder decision D-013: product code lives in its own repository, `appelkas-stack/fable`).

The GitHub integration in this session cannot create repositories (403), so the real repo could not be pushed yet. This snapshot (of commit `f7821ad`, task 0.1) exists only so the work survives the ephemeral build environment.

**To migrate:** create `appelkas-stack/fable` on GitHub → push the real history from the working copy (or, if lost, `git init` from this directory) → delete `fable-staging/` from this repo.
